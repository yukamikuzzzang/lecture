파티션 부분은 건들지 마세요.

네트워크 및 호스트: 연결 되어있지 않음 > 켬 으로 바꿔놓자.(항상 연결될 수 있도록)
gnome을 쓸 것이기에 KDUMP쓰지 않을 것임

시간 및 날짜: 우리나라로 변경해주기

root 암호		> 정보보안
user creation
사용자 생성 부분임

성명, 사용자 이름 yukamikuzzzang
암호: miku~

<
겨우 다운받아놨더니 D:다운로드 폴더가 access가 안되는 기현상을 맛보고 있는 오늘이 레전드..
>

!설치를 완료한 후 하드디스크가 우선적으로 작동하기 위해 순서를 변경해주기!
안그러면 계속해서 재설치를 요구함

자기계정으로 자신이 설정한 비밀번호를 친 다음 들어가주기

centos 접속 후 terminal 접속하기!

다행스럽게? 알아서 부팅디스크를 잡아주었다.


root 접속 방법: 
su
사용자계정이 루트로 간 것으로 보임
root@localhost 사용자명

su -
root@localhost ~

(강의 PPT 참고: with 솔리오스)

1. 설치 후 먼저 할 일
*리눅스 설치시 자동 생성되는 계정 중 사용하지 않는 계정 제거
*root 계정의 접속 시간 제한
*리눅스 싱글 사용자 모드 제한
:root 비밀번호를 까먹은 경우. single 사용자 모드로 가능했었음.)
single 사용자 모드로 비번 없이 막 들어갈 수 있었었음.
*shadow password는 꼭 사용해야함.
(지금 사용 중인 것이 shadow password)
*쉘을 이용한 기록관리;history
유닉스, 솔라리스에서 지원 안했던 기능이었으나..

>사용하지 않는 계정 삭제
*/etc/passwd, /etc/shadow, /etc/group에서 확인
'절대 경로'로써 기록해야함!
*lp, adm, sync, shutdown, halt, news, uucp, operator, games, rpc, rpcuser etc.
(지금은 잘 사용하지 않거나 원격 관련된 것들임,rpc/rpcuser)

vi > dd(한 줄씩 제거)

vi .exrc
[set nu
set ai]

>사용하지 않는 계정과 그룹 중지
*userdel -r, groupdel 사용
*주석(#)처리
*/etc/shadow에서 *와 !!의 차이

*
id/pw에 의한 로그인 불가
ssh-key 기반 방식은 가능; 실제로는 안된다고 함?!

!!
어떠한 방법으로든 login이 안됨

/etc/shadow에 적혀있는 사용자계정의 암호화 되어있는 비밀번호
조금만 바꾸어도 접속할 수 없게 만들 수 있음


*/etc/passwd에서 적용
계정 끝에다가 /sbin/nologin, /bin/false을 붙이면 접속할 수 없음
로그인 가능 계정은 /bin/bash
(보안 때문에 필요하다 이말이야.)

>root 계정의 접속 시간 제한
root 로그인한 상태에서 관리자는 자리를 비우지 않아야 함.
자리를 비우면 자동 로그아웃 하도록 설정

*/etc/profile에서 설정
46,47번 줄에

TMOUT = 300(초)
export TMOUT

리눅스 내의 명령어는 아님.
환경설정 명령어를 실행하겠다고 입력해야함.
TMOUT=300
export TMOUT

현재 시스템에 내가 기록한 것을 실행시키고 싶다면
# source /etc/profile
. /etc/profile

"/etc/profile"도 기억해두기

>Shadow Password 꼭 사용
현재 정책에선 Shadow Password를 쓰고 있음
확인법: ls /etc/password

*pwconv 명령어
리눅스 시스템에 패스워드 정책을 shadow 패스워드 정책을 적용하도록 하는 명령어

*pwunconv
일반 패스워드 정책으로 변경

000  000    000
User Group Other

pwunconv
/etc/passwd에 비밀번호가 보이게 됨. 
해당 파일에 권한은 모든 유저가 읽을 수는 있음.
일반 사용자 뿐 아니라 root의 비밀번호까지 보임

pwconv
아까보였던 비밀번호가 전부 보이지 않게 됨.

>쉘을 이용한 기록관리(history)
리눅스는 그냥 shell
예는 bash shell
cd /etc/skel

ls -al(숨겨진 폴더 공개)
.bash_logout
.bash_profile
.bashrc

ls -al /home/사용자명
위의 3가지 것들이 모두 포함되어있음.

useradd cyber
passwd cyber
1234
(루트는 비밀번호 조건에 맞지 않아도 변경가능)

/etc/skel > 거기에 사용자계정을 만들면 거기에 있는 내용이 자동으로 복사
a.text를 만들기 전에 생성된 사용자계정은 적용되지 않음

그래서 cyber는 a.text가 생성.
초창기에는 사원번호별로 아이디 생성 후 비밀번호 변경을 요구했음.
